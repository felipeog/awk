name: Test

on:
  workflow_dispatch: {}
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-23.10
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: |
          npm ci
          npm test
